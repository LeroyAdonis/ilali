# Phase 2: Data Source Migration — Completion Report

**Date:** 2026-07-29  
**Status:** ✅ Complete — `npm run build` passes with zero errors (55 pages, all routes green)

## Summary

All ~8 core files plus 5 additional files converted from `constants.ts` static imports to live DB queries via Neon + Drizzle. DB-to-UI mapper layer created. Zero UI changes — all existing types, component props, and visual output preserved.

## Files Created
- `src/lib/db/mappers.ts` — `mapProvider()`, `mapVenue()`, `mapProviders()` functions
- `src/lib/db/seed-data.ts` — seed data extracted from constants.ts (used only by seed.ts)

## Files Modified (13 total)

### Task Brief Files (T012-T018)
| Task | File | Change |
|------|------|--------|
| T012 | `src/app/activity/[slug]/page.tsx` | `providers.find()` → `getProviderBySlug()` + mapper; `generateStaticParams` now async DB |
| T013 | `src/app/venues/[slug]/page.tsx` | `venues.find()` → `getVenueBySlug()` + mapper; `generateStaticParams` now async DB |
| T014 | `src/app/browse/page.tsx` | Server fetches all providers+venues from DB; passes mapped arrays as props to `ResultsSection` |
| T015 | `src/app/providers/page.tsx` | Server fetches providers+categories, passes to client as props |
| T015 | `src/app/providers/client.tsx` | Accepts `providers` + `categories` props instead of importing from constants |
| T016 | `src/app/venues/page.tsx` | Server fetches venues, maps, passes to client as props |
| T016 | `src/app/venues/client.tsx` | Accepts `venues` prop instead of importing from constants |
| T018 | `src/components/FilterBar.tsx` | Accepts optional `categories` prop; falls back to constants import for backward compat |

### T017/T019 — No Changes Required
- `src/app/locations/page.tsx` — uses hardcoded array, no DB dependency
- `src/components/SearchBar.tsx` — URL-based, no constants import

### T020 — Constants Cleanup
- `src/lib/constants.ts` — Removed `providers` and `venues` arrays; added deprecation comment pointing to DB queries/mappers

### Additional Files Fixed (build dependencies)
| File | Change |
|------|--------|
| `src/app/home/page.tsx` | Now async — fetches providers + venues from DB, maps, passes to components |
| `src/app/category/[slug]/page.tsx` | Fetches filtered providers from DB via `getProviders({category: slug})` |
| `src/app/api/providers/route.ts` | Uses `getProviders()` + mapper instead of constants import |
| `src/app/api/venues/route.ts` | Uses `getVenues()` + mapper instead of constants import |
| `src/app/api/search/route.ts` | Uses `getProviders()` + mapper instead of constants import |
| `src/lib/db/seed.ts` | Imports seed data from `./seed-data.ts` instead of removed constants exports |

## Mapper Behavior
- **Provider mapper:** `imageUrl`→`image`, `priceValue` (cents)→`price` (formatted "R150"/"Free"), `ageMin+ageMax`→`ageRange` ("12–15 years"), `category` (slug) → looks up display name from categories array, `distance`→"—" (not in DB)
- **Venue mapper:** `imageUrl`→`image`, `capacity` (int)→`"50 people"` (string or undefined), `amenities` preserved from `getVenueBySlug()` join

## Build Result
```
✓ Compiled successfully (9.6s)
✓ TypeScript check passed (9.9s)
✓ Generated 55/55 static pages (1752ms)
All routes: ○ static / ● SSG / ƒ dynamic — all green
```

## Issues Encountered
1. **Seed file broke build** — `seed.ts` imported `providers`/`venues` from constants after removal. Fixed by extracting seed data to `seed-data.ts`.
2. **5 additional files imported from constants** — `home/page.tsx`, `category/[slug]/page.tsx`, and 3 API routes still imported `providers`/`venues`. All converted to DB queries.
